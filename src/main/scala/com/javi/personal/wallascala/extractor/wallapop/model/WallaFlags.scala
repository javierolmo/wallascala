package com.javi.personal.wallascala.extractor.wallapop.model

case class WallaFlags(
                       pending: Boolean,
                       sold: Boolean,
                       reserved: Boolean,
                       banned: Boolean,
                       expired: Boolean,
                       onhold: Boolean
                     )
